# This file is the entry point to configure your own HTTP routes.
# Files in the routes/ subdirectory configure the routes for your dependencies.

# qobd_web_agent_home:
#     path: /agent
#     defaults: { _controller: 'App\Controller\AgentController::index' }
